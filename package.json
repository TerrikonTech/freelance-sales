{
  "name": "freelance-sales-v2",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "build": "npm run build -w apps/web && npm run build -w apps/api",
    "test": "npm run test -w apps/api && npm run test -w apps/web"
  }
}
